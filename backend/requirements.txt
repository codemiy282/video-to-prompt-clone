# Backend Python Dependencies
# For LTX-Video integration with RTX 5070 optimization

# IMPORTANT: PyTorch must be the CUDA build, NOT the default +cpu wheel.
# The --extra-index-url below points at PyTorch's CUDA 13.0 wheel host.
# (RTX 5070 = Blackwell; driver supports CUDA 13.3, so cu130 runtime is fine.)
--extra-index-url https://download.pytorch.org/whl/cu130

# Core ML Libraries (pinned to the CUDA 13.0 wheels)
torch==2.13.0+cu130
torchvision==0.28.0+cu130
torchaudio==2.11.0+cu130

# Diffusion & Transformers
diffusers>=0.28.2
transformers>=4.47.2,<4.52.0
safetensors>=0.4.0
accelerate>=0.17.0  # required by pipeline.enable_model_cpu_offload()

# HuggingFace
huggingface-hub>=0.30.0
tokenizers>=0.14.0

# Image & Video Processing
pillow>=10.0.0
opencv-python>=4.8.0
imageio[ffmpeg]>=2.23.0
av>=11.0.0

# API Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiofiles>=23.2.0
python-multipart>=0.0.6

# Utils
numpy>=1.24.0
scipy>=1.11.0
einops>=0.7.0
timm>=0.9.0
sentencepiece>=0.1.96

# Optional: Development & Debugging
python-dotenv>=1.0.0
rich>=13.7.0  # Better logging
